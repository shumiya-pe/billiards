# SPEC.md — ナインボール 仕様書

---

## 1. アプリ概要

ビリヤードのナインボールを題材にした、子ども向けのゲームアプリ。
タップ＆ドラッグの直感操作で球を撞いて遊ぶ。
本アプリは「まずゲームの土台を作り、あとから教育要素（数の順番・英語・足し算など）を
付け足していく」ことを前提とした **ゲームベース** の実装である。

---

## 2. 対象ユーザー

小学校低学年〜中学年（1〜4年生中心）。
将来的に教育要素を載せることを想定。

---

## 3. テーマカラー

- UIアクセント：`#1565c0`（ダークブルー／緑フェルトに映える、シリーズ共通）
- フェルト：`#1f7a44`（ビリヤードグリーン、PWA theme_color）
- レール（木枠）：`#6d4726`
- 背景：`#FFF8E7`（シリーズ共通クリーム）

---

## 4. ゲーム仕様

### ルール（ナインボール簡易版）

- 1〜9番のボールをラック（ダイヤモンド）に配置。手球（白）で撞く。
- 常に **盤上で一番小さい番号** を最初に当てるのが正規ショット。
- どの順でも、正規ショットでポケットに入ればそのまま落とした扱い（コンビネーション可）。
- **9番を正規ショットで落としたらクリア（勝ち）**。
- ファウル（下記）の場合は手球フリー（ボールインハンド）で置き直し：
  - スクラッチ（手球をポケットに落とす）
  - 最初に当てた球が最小番号でない
  - どの球にも当たらない
- ファウルで9番が落ちた場合は9番をリスポット。

### 2人モード（ふたりで こうたい）

⚙️設定「あそぶ にんずう」で選択（既定は ひとり）。2人パス方式：

- プレイヤー1から開始。正規ショットでボールをポケットに入れたら同じプレイヤーが続行。
- 何も入らなかったら交代。ファウルでも交代し、次のプレイヤーがボールインハンド。
- 勝ちボール（最後のボール）を正規ショットで落としたプレイヤーの勝ち。
- 各ボールに「どのプレイヤーが落としたか」を記録し、ボールトレイと結果画面に表示。

### 表示モード（ボールのラベル）

ルール（プレイ順＝order 1..9）は共通のまま、ボールに描く文字だけを切り替える。

| モード | 表示 | 備考 |
|---|---|---|
| すうじ | 1〜9 | 既定。9番がマネーボール（ストライプ） |
| えいご | A〜Z の連続9文字 | 開始文字をランダム選択し、そこから順番に9文字（A..R始まり）。order1＝先頭文字、order9＝9文字目 |

英語モードの大小文字は ⚙️設定で選択：

- 大文字（ABC）／ 小文字（abc）／ ミックス（1球ごとにランダムで大小）

順番（最小→最大）はアルファベット順に一致するので、「次に狙う文字」がそのまま学習対象になる。

### 操作（コア入力ループ）

1. **タップ**：手球の置き場所を決める（ボールインハンド時）。
2. **ドラッグ**：手球から引く向き・距離で「角度」と「パワー」を調整（スリングショット式：引いた逆向きに飛ぶ）。
3. **リリース**：指を離すとショット。

### 物理（自前・`src/billiards.js`）

- 固定タイムステップ（1/240s）＋アキュムレータで描画レート非依存。
- ころがり摩擦（等減速）、クッション反射（反発係数）、ボール同士の等質量弾性衝突＋重なり解消。
- ポケットは6個（4隅＋長辺中央2）。吸い込み半径で判定。
- 手球の最初の接触球を記録してファウル判定に使用。

---

## 5. UI設計

- ヘッダー：透明背景・アプリ名（ダークネイビー）・白丸の ℹ️/⚙️ ボタン（シリーズ共通）。
- ステータスバー：次に狙うボール（番号チップ）・ショット数・ファウル数。
- メッセージ行：状況に応じたヒント／フィードバック（ひらがな）。
- ボールトレイ：盤面上の余白に全ボールを order 順に並べて表示。残っている球はカラー、
  落とした球は薄表示＋バッジ（1人＝✓、2人＝落としたプレイヤー番号の色付きバッジ）。
- プレイヤーバー（2人モードのみ）：各プレイヤーの落とした個数と現在の手番（▶ばん）を表示。
- プレイエリア：Canvas（木枠＋緑フェルト＋6ポケット）。縦持ち2:1テーブル。
- 照準表示：予測ライン（破線）＋キュー（引き量に連動）＋パワーメーター。
- 手球配置：ゴースト表示（置けない場所は赤）。
- クリア時：🏆 オーバーレイ＋「もういちど」。

---

## 6. スコア・進捗設計

- 1ゲーム＝1〜9番をクリアするまで。スコアは「ショット数」「ファウル数」で記録。
- 少ないショット・少ないファウルを目指す（タイムアタック的な遊び方）。

---

## 7. 設定項目

- ⚙️ 設定パネル：バージョン番号表示／「さいしょから やりなおす」。

---

## 8. 将来の拡張（教育要素）

- ~~**英語**：ボールのラベルをアルファベット表示（大文字／小文字／ミックス）。~~ → v1.1.0 実装済み
- **読み上げ**：数字・アルファベットの読み上げ（Web Speech API）。
- **数の順番**：1→9 / A→ を意識させるナビ強化。
- **計算モード**：落とした番号で足し算・かけ算ミニ問題。
- ~~**しょうぶモード**：追加する場合は COMMON.md §8（2人パス方式）を参考にする。ただし本アプリはゲームベースのため、クイズ系仕様はそのまま適用しない。~~ → v1.8.0 で「ふたりで こうたい」として実装済み（§4 参照）。
- **Google Play 配信**：シリーズ共通方針に合わせて Capacitor Android で AAB 化する。
